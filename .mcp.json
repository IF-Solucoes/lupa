{
  "mcpServers": {
    "lupa": {
      "command": "python3",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/server/lupa_mcp.py"
      ],
      "env": {
        "LUPA_INDEXES": "${LUPA_INDEXES}"
      }
    }
  }
}
